describe("extensions-proto3-test", () => {

  it("generates readable extension values for services")
  it("generates readable extension values for methods")
  it("generates readable extension values for messages")
  it("generates readable extension values for fields")

});
